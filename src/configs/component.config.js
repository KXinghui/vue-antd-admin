/**
 * 优雅注册组件 使用组件库时，一般都是按需加载
 */
// import Vue from "vue";
/* import {} from "ant-design-vue";

const components = {};

export const handler = {
  install(Vue) {
    Object.keys(components).forEach(key => Vue.user(components[key]));
  }
}; */
